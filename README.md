# React study guide
# Installation
  asdasd
  asdasd
#Map an array in reverse order:
  {cardsData.map((card, index)=>
      <Card 
          info={ card } 
          key={ card._id }
          actualizarLista={actualizarLista}
      />
  ).reverse()} 
